import { AlignJustify } from "lucide-react";
import { FC } from "react";

interface Props {
  className?: string;
}

export const MobileWrapepr: FC<Props> = ({ className }) => {
  return <AlignJustify className={className}>
    
  </AlignJustify>;
};
