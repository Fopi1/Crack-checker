import { Container } from "@/shared/components/shared";

export default function NotFound() {
  return (
    <div className="uppercase w-full h-screen">
      <Container className="flex items-center justify-center h-full">
        404 | not found
      </Container>
    </div>
  );
}
